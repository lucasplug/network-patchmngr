[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-ra"
markers = [
  "no_admin: start deze test zonder aangemaakte beheerder",
  "e2e: draait de app in een echte browser (vereist Playwright)",
]
